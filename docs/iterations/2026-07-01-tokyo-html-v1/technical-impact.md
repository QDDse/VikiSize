# 本轮技术影响：东京旅行模板 v1.0

> 状态：源技术分析。已由 v1.0 聚合迭代收口。

- 新增旅行模板列表、详情、H5 预览和原生编辑器页面边界。
- 从 HTML 内联 `trip` 提取版本化 JSON，消除两份模板漂移。
- H5 变为构建产物，不再是数据源本身。
- 正式环境需要可完成微信业务域名校验的 HTTPS 托管。
- 扩展旅行实例节点字段、模块字段、地图坐标和附件关联。
- 新增模板列表、实例幂等创建、节点增删改排等云函数接口。

完整方案见 `docs/versions/v1.0/travel-technical-design.md`。
