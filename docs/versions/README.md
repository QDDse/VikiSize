# VikiSize 版本文档

此目录按产品版本收集已经确认的需求范围、PRD、技术方案和验收标准。

- `docs/specs/`：持续更新的权威 PRD 和技术方案。
- `docs/iterations/`：每一轮调研、讨论和需求变化过程。
- `docs/versions/`：某个版本在进入实现时的范围快照和文档入口。

版本目录使用 `v主版本.次版本` 命名，例如 `v1.0/`。
