## 变更说明

<!-- 这个 PR 做了什么、为什么 -->

## 关联

<!-- 关联的 Issue / 规格（docs/specs/）/ 迭代记录（docs/iterations/） -->

## 完成定义检查

- [ ] `npm run lint` 通过
- [ ] `npm test` 通过
- [ ] `npm run validate:wechat` 与 `npm run validate:travel-templates` 通过
- [ ] 行为变更已同步到对应规格文档（如适用）
- [ ] 云函数与本地适配器（localStore）外部行为保持一致（如适用）
- [ ] 微信开发者工具手动验证（如涉及页面/交互变更）
