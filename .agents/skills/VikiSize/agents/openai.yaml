interface:
  display_name: "VikiSize"
  short_description: "Repo-specific patterns and workflows for VikiSize"
  default_prompt: "Use the VikiSize repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true